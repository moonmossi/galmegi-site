# 빠른 GI 검증

2026-09-10, Godot 4.7.1 Forward+, Blender 5.2 Cycles GPU, GTX 1660 Ti.

- 동일 실내 67개 메시, 75개 프로브. 화면 1280×900 유지.
- 라이트맵 텍셀 간격 0.065m → 0.3m, 총 픽셀 210080 → 69696 (66.8% 감소). 샘플 256 → 32.
- 이번 bake 호출부터 적용 완료까지 69.978초. 정적 베이크·EXR 저장 38.520초, 디노이즈 16.210초, Blender 작업 전체 55.753초. 이전 전체 소요 시간은 미측정이며 속도 배수 비교는 하지 않는다.
- 정적 메시를 한 번의 bake 호출로 처리하고 각 EXR에 Blender HDR 디노이즈를 적용한다. 원본도 보존한다. 프로브는 기존 방식이다.
- 실제 Godot 렌더에서 벽·천장의 큰 얼룩 감소를 확인했다. 가이드 없는 디노이즈와 낮은 해상도로 작은 명암이 흐려지고 국소 얼룩이 남는다. Cycles 전체 렌더와 동일한 결과는 아니다.
- 결과: baked.tscn, denoised_full.png, denoised_indirect.png. 같은 저해상도 원본: raw_full.png, raw_indirect.png. 수치와 원본 작업 경로: metrics.json.
- 기본 버튼은 32샘플과 자동 디노이즈를 사용한다. 공용 텍셀 설정 기본값은 0.25m이며 이 비교 실행은 0.3m이다.
- 검증 실행 종료 시 렌더링 리소스·ObjectDB 경고가 남았다. CITY 전체·실제 캐릭터·프레임 성능은 검증하지 않았다.
